# prometheus.dev.yml
global:
  scrape_interval: 15s
  evaluation_interval: 15s
scrape_configs:
  - job_name: 'neural-nexus-db'
    metrics_path: /metrics
    static_configs:
      - targets: ['neural_nexus_db_api:8080']
        labels:
          app: neural-nexus-db
  # - job_name: 'app2'  # Add more endpoints as needed
    # metrics_path: /metrics
    # scheme: https
    # static_configs:
    #   - targets: ['app2-xyz.a.run.app']
    #     labels:
    #       app: app2